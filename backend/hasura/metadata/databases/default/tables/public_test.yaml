table:
  schema: public
  name: test
object_relationships:
  - name: user
    using:
      manual_configuration:
        remote_table:
          schema: keycloak
          name: user_entity
        insertion_order: null
        column_mapping:
          keycloak_id: id
select_permissions:
  - role: user
    permission:
      columns: []
      filter:
        keycloak_id:
          _eq: X-Hasura-User-Id
