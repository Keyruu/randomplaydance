table:
  schema: keycloak
  name: keycloak_role
array_relationships:
  - name: user_role_mapping
    using:
      manual_configuration:
        remote_table:
          schema: keycloak
          name: user_role_mapping
        insertion_order: null
        column_mapping:
          id: role_id
